project_name:
  type: str
  help: What is your project name?

use_gpu:
  type: bool
  help: should a GPU?
  
accelerator:
  type: str
  help: Which GPU to test?

python_version:
  type: str
  help: which python version to test?